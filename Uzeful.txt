https://www.youtube.com/watch?v=f4s1h2YETNY&t=3s 
https://www.youtube.com/watch?v=KN9nHo9kvZs

prostředí, ve kterým se bude spouštět fragment shader
fragment shader = neuronka

Vulkan:
    Create Vulkan Instance
        app info
        validation layers
        Determine Physical device
        Queue family
    Allocating memory
        Create Buffers
        Query Memory Types
        (Staging Buffer)
        Allocate Memory
        Write Data to Memory
        Bind Buffers to Memory
    Create Pipeline
        Descriptor Pool
        Descriptor Sets
        Update descriptor sets to use our buffers
    Submit work to GPU
        Command Pool
        Command Buffer
        Record Commands
        Submit Work
        Wait
    Cleanup
    
GIT
    Run agent
        eval "$(ssh-agent -s)"
    Add key to agent
        ssh-add ~/.ssh/GitHubNEAT
        
OpenCL compile
    g++ -std=c++0x -o Scratch Scratch.cpp -lOpenCL -D __LINUX__
